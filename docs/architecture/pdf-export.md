# PDF Export

PDF generation is out of scope for the first phase.

Current behavior:

- Build PDF preview components in `src/components/pdf`.
- Keep preview components tokenized.
- Do not introduce PDF rendering dependencies until export requirements are fixed.

