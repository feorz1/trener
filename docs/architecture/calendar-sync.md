# Calendar Sync

Calendar sync is out of scope for the first phase.

Current behavior:

- Use mock schedule data.
- Keep slot/status types explicit.
- Do not couple UI components to a future calendar provider.

Future providers must map into app-owned calendar types before reaching UI components.

