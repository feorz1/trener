# Form Layout

## Metadata

- Tier: patterns

## Rules

- Labels use `theme.typography.body.smStrong` or `theme.typography.caption`.
- Inputs use canonical input components.
- Error text must be close to the field.
- Vertical gaps use `theme.spacing.sm`, `theme.spacing.md`, or `theme.spacing.lg`.

