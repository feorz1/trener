# Task 04: Build Badge And Button

Goal: create canonical status and action primitives.

Badge is already created in Figma:

- component set: `Badge`
- node: `89:596`

Next steps:

1. Implement `src/components/ui/Badge.tsx` to match Figma.
2. Add `docs/design-system/components/badge.md`.
3. Add dev-kit examples.
4. Repeat pipeline for `Button`.

